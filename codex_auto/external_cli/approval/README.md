# External Codex approval package

This directory contains a pending approval request only for a future external Codex CLI run of `PMBOT-BATCH-001`.

It does not execute Codex.
It does not allow external Codex CLI now.
Human approval must be separate, explicit, and recorded later before any future execution can be considered.

The approval request fixes the approved prompt path, command preview path, allowed output paths, forbidden paths, expected tests, rollback plan, and mandatory post-execution Flocky validation.

Runtime wiring remains forbidden.
Wallet usage, API usage, private key usage, trading, live Polymarket access, and real orders remain forbidden.

Even after any future human approval, post-execution Flocky validation is still mandatory before any follow-up governance step.
